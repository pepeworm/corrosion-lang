import { useState, useContext } from "react";

export default function Home({ params, sections }) {
	return (
		<section className="pt-20">
			<div className="text-center">
				<h1 className="gradient header">{ctx["Compiling"][0][0]}</h1>
			</div>
		</section>
	);
}
